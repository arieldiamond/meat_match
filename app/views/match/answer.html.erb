<div id="match">

	<h1>Can you <strong><%= @technique.name %></strong> a <strong><%= @meatcut.name %></strong>?</h1>

	<% if @match == nil %>
		<div id="match_no">
			<h2>I WOULDN'T DO THAT <br>IF I WERE YOU.</h2><br>
			<p>Where there's a will, there's a way, but there may be a better way.</p>
		</div>
	<%= button_to 'I give up. Auto-Match Me!', match_matchme_path(params[:meatcut]), :class => "button", :method => :get %>
		<%= button_to "Try again!", root_path, :class => "button", :method => :get %>
	<% elsif @match.good_idea == "yes" %>
		<div id="match_yes">
			<h2>YES YOU CAN!</h2>
		</div>
			<br><%= button_to "Try again!", root_path, :class => "button", :method => :get %>
	<% else %>
		it ain't working.
	<% end %>



	<div id="match_technique">
		<table class="pure-table pure-table-horizontal">
			<thead>
				<tr>
					<th><h3>About <% if @technique.name.split("").last == "e" %>
						<%= @technique.name[0...-1].capitalize %>ing
						<% else %>
							<%= @technique.name.capitalize %>ing
						<% end %>
						</h3>
					</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td><strong>Description:</strong> <%= @technique.description %></td>
				</tr>
				<tr>
					<td><strong>Is it a dry or wet technique? </strong>
					<% if @technique.dry == true %>
						<%= "Dry" %></td>
					<% else %>
						<%= p "Wet" %></td>
					<% end %>
				</tr>
				<tr>
					<td><strong>Equipment needed: </strong><%= @technique.equipment.capitalize %></td>
				</tr>
			</tbody>
		</table>
	</div>

	<div id="match_meatcut">
		<table class="pure-table pure-table-horizontal">
			<thead>
				<tr>
					<th>
						<h3>About <%= @meatcut.name.capitalize %>s</h3>
					</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td>
						<strong>What is it?</strong> <%= @meatcut.description %>
					</td>
				</tr>
				<tr>
					<td>
						<strong>Where is it from on the <%= @meatcut.animal %>?</strong> The <%= @meatcut.primal %>
					</td>
				</tr>
				<tr>
					<td>
						<strong>May also be called:</strong> <%= @meatcut.alt_names %>
					</td>
				</tr>
			</tbody>
		</table>
	</div>
</div>